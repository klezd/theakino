import React from 'react';
import styles from './Header.module.css';

const Header = (props) => <div className={styles.root} />;

Header.defaultProps = {};

Header.propTypes = {};

export default Header;
